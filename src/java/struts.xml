<!DOCTYPE struts PUBLIC
"-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
"http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>
    <!-- Configuration for the default package. -->
    
          <package name="default"  extends="struts-default">
        <action name="SaveAct" class="action.saveAction">

            <result name="SUCCESS">index.jsp</result>  <!-- Redirect to success.jsp after save -->
            <result name="ERROR">index.jsp</result>    <!-- Redirect to error.jsp in case of failure -->
 
        </action>
        
    
    
    <action name="displayUsers" class="action.DisplayAction">
        <result name="success">/display.jsp</result>
    </action>

<action name="exportHtml" class="action.ExportHtmlAction">
    <result name="success" type="stream">
        <param name="contentType">text/html</param>
        <param name="inputName">fileInputStream</param>
        <param name="contentDisposition">attachment;filename=user_list.html</param>
        <param name="bufferSize">1024</param>
    </result>
</action>

        
        
        
        
        
        
    </package>



</struts>